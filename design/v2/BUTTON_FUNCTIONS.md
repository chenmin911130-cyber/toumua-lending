# V2 全按钮与交互清单

当前共 **196 项交互定义**，覆盖全部42张页面及共用控件。多个同义入口或重复按钮可在一项中列出，但每个实际入口都要验证。所有项目当前均为 **not_implemented / not_run**。

`controls.json` 是机器可读交付；`controls.csv` 可用于任务看板。每个ID的验收项使用 `CTL-<ID>`，Cursor填写实现路径和测试路径，不能凭目测标为通过。清单是目标需求，不是自动生成的“测试已通过”报告。

## 适用规则

- 每个页面继承所有适用 GLOBAL 项：导航、搜索、筛选、分页、标签、弹窗、关闭、返回、密码可见性、上传、打印、错误等；图里相同动作无需重复写相同逻辑。
- 只读表格文字、状态徽章、时间线、静态卡片不是按钮；若实现为链接必须按对应跳转条目执行。图中生成偏差不构成新增角色或政策。
- 所有按钮同时考虑可见、可用、处理中、成功、明确失败、结果未知状态。禁用必须有文字原因，不能一直禁用当作实现。
- 前后端都做权限和状态校验。所有写入成功以服务端持久化为准，刷新可验证。
- 业务规则未确定导致的阻塞需要在 DECISIONS.md 和实现状态报告记录，不能伪造计算结果。
- 此脚本只用于最初生成设计清单；Cursor开始填写状态后不要重新运行覆盖进度。

## GLOBAL · 全局复用控件

### GLOBAL-01 · 品牌 Logo

- **权限：** 全部
- **条件／校验：** 无
- **动作／目标：** 公开回 /；客户回 /customer；员工回 /staff
- **验收（CTL-GLOBAL-01）：** 在三种外壳点击均进入正确首页
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-02 · 客户导航 My overview / Application / Repayments / My security / Help

- **权限：** Customer
- **条件／校验：** 业务页需已验证并关联；无贷款时显示空状态
- **动作／目标：** 按路由表跳转；多笔贷款先选 loanId，不擅自默认另一笔
- **验收（CTL-GLOBAL-02）：** 五个入口可键盘操作；选中状态随真实路由变化
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-03 · 员工导航 Overview / Borrowers / Applications / Loans / Collateral / Transactions

- **权限：** Staff
- **条件／校验：** 按 IMPLEMENTATION_SPEC 权限矩阵
- **动作／目标：** 进入各资源列表；不可访问项不显示，直接输入URL仍鉴权
- **验收（CTL-GLOBAL-03）：** 逐角色核对菜单及后端403/404；不会显示客户外壳
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-04 · Administration / Staff accounts / Activity log

- **权限：** manage_staff 或 view_audit
- **条件／校验：** 相应独立权限
- **动作／目标：** 打开管理子导航至S16/S19
- **验收（CTL-GLOBAL-04）：** 只有相应授权项可见且API独立校验
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-05 · Settings / Your account

- **权限：** 已登录
- **条件／校验：** 有效会话
- **动作／目标：** 进入A02；员工继续用员工外壳
- **验收（CTL-GLOBAL-05）：** 角色只读，不存在自助提升权限控件
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-06 · 头像 / 姓名 / 下拉箭头

- **权限：** 已登录
- **条件／校验：** 有效会话
- **动作／目标：** 打开菜单含Your account、Sign out；外部点击或Esc关闭
- **验收（CTL-GLOBAL-06）：** 焦点与键盘导航正确，菜单不遮住关键操作
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-07 · Sign out

- **权限：** 已登录
- **条件／校验：** 提交中先说明尚未确认操作并保留服务端attempt
- **动作／目标：** POST /auth/logout；清空受保护缓存后到C02
- **验收（CTL-GLOBAL-07）：** 退出后后退及直接API均不能读取旧数据
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-08 · 顶部放大镜 / Ctrl或Cmd+K

- **权限：** Staff
- **条件／校验：** 至少2字符；资源范围鉴权
- **动作／目标：** 打开全局搜索对话框，GET /search?q=；选择结果到对应详情
- **验收（CTL-GLOBAL-08）：** 不同角色结果范围正确，旧响应不覆盖新输入
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-09 · 全局搜索结果 / Enter

- **权限：** Staff
- **条件／校验：** 结果仍有权限且存在
- **动作／目标：** 进入结果记录；关闭搜索框
- **验收（CTL-GLOBAL-09）：** 无权限或已删除记录给404安全页，不泄露摘要
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-10 · 通知铃铛 / 未读数字

- **权限：** 已登录
- **条件／校验：** 有效会话
- **动作／目标：** GET /notifications，打开G02
- **验收（CTL-GLOBAL-10）：** 未读数来自服务端；无通知为0而不是固定红点
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-11 · Back / 面包屑 / Return to overview

- **权限：** 当前页访问者
- **条件／校验：** 未保存内容先处理脏表单；未知资金结果保留attempt
- **动作／目标：** 返回规范父路由，列表筛选保留；不依赖可能离开站点的history.back
- **验收（CTL-GLOBAL-11）：** 新标签直接打开详情后返回仍正确
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-12 · Cancel / Close / X / Esc

- **权限：** 当前页访问者
- **条件／校验：** 提交中不撤销已发送业务；未保存时提示
- **动作／目标：** 关闭抽屉或确认框；返回触发点
- **验收（CTL-GLOBAL-12）：** 取消不产生API写入；焦点回触发控件
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-13 · Keep editing

- **权限：** 表单操作者
- **条件／校验：** 脏表单离开提示
- **动作／目标：** 关闭提示保留表单全部未保存输入
- **验收（CTL-GLOBAL-13）：** 输入仍在，当前路由不变
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-14 · Discard changes

- **权限：** 表单操作者
- **条件／校验：** 脏表单离开提示
- **动作／目标：** 丢弃尚未保存部分并继续离开；不删除服务器草稿
- **验收（CTL-GLOBAL-14）：** 已保存草稿仍能重新打开
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-15 · 表内搜索框 / 搜索图标

- **权限：** 列表访问者
- **条件／校验：** q长度≤160；查询范围鉴权
- **动作／目标：** 按当前资源GET查询，防抖300ms并重置分页
- **验收（CTL-GLOBAL-15）：** 输入后真实结果改变；清空回默认结果
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-16 · 状态标签 / Status下拉 / Type下拉

- **权限：** 列表访问者
- **条件／校验：** 值来自资源白名单
- **动作／目标：** 更新URL筛选及当前资源GET查询
- **验收（CTL-GLOBAL-16）：** 列表、总数、选中标签一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-17 · Date range / 日期选择器

- **权限：** 列表访问者
- **条件／校验：** from≤to；按Auckland业务日
- **动作／目标：** 打开日期范围选择并提交查询；清空撤销范围
- **验收（CTL-GLOBAL-17）：** 边界日记录正确，不以UTC午夜误删
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-18 · 排序表头 / 上下箭头

- **权限：** 列表访问者
- **条件／校验：** 只能选择后端允许排序字段
- **动作／目标：** 切换升降序并重置分页
- **验收（CTL-GLOBAL-18）：** 跨页全局排序正确而非只排当前数组
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-19 · Previous / Next / 页码 / Page size

- **权限：** 列表访问者
- **条件／校验：** 页大小20或50；不存在页禁用
- **动作／目标：** 按真实游标／页索引加载，保留筛选
- **验收（CTL-GLOBAL-19）：** 无重复或漏页；末页Next禁用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-20 · Clear filters

- **权限：** 列表访问者
- **条件／校验：** 无结果或有活动过滤器
- **动作／目标：** 清空查询和过滤器，回第一页
- **验收（CTL-GLOBAL-20）：** 输入和URL同步清空，重新查询
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-21 · 表格行 / 右箭头 / View details

- **权限：** 列表访问者
- **条件／校验：** 对象级权限
- **动作／目标：** 进入该行详情或抽屉，路由带真实ID
- **验收（CTL-GLOBAL-21）：** 每一行打开自己的记录，不固定跳示例ID
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-22 · Tabs / 标签页

- **权限：** 当前页访问者
- **条件／校验：** 标签本身可访问
- **动作／目标：** 更新tab查询，加载对应内容
- **验收（CTL-GLOBAL-22）：** 刷新保留标签；键盘左右和Enter可操作
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-23 · Show password / Hide password / 眼睛图标

- **权限：** 密码表单使用者
- **条件／校验：** 无
- **动作／目标：** 仅切换该字段可见性，不改值、不提交
- **验收（CTL-GLOBAL-23）：** 各字段独立；aria-label随状态更新
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-24 · Select / 下拉箭头 / 清除选项

- **权限：** 表单操作者
- **条件／校验：** 选项由后端或契约白名单提供
- **动作／目标：** 展开、选择、清除；必填项清除后报字段错误
- **验收（CTL-GLOBAL-24）：** 鼠标键盘行为一致；不能注入不存在角色或ID
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-25 · Upload photos / 选择文件 / 重试上传

- **权限：** 资产编辑者
- **条件／校验：** JPEG/PNG≤10MB每张，每件≤10张；资产可编辑
- **动作／目标：** POST /assets/:id/attachments；显示进度、失败、已上传；私有文件存储
- **验收（CTL-GLOBAL-25）：** 伪扩展名与越权上传被拒绝，刷新可见已保存附件
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-26 · 照片缩略图 / 放大 / 上一张 / 下一张

- **权限：** 记录访问者
- **条件／校验：** 有该文件读取权限
- **动作／目标：** GET /private-files/:id；打开图片查看器
- **验收（CTL-GLOBAL-26）：** 跨客户文件ID不可访问；图集首尾按钮正确禁用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-27 · Remove photo / 缩略图X

- **权限：** 资产编辑者
- **条件／校验：** 草稿附件可解绑，已批准历史不可删
- **动作／目标：** 确认后DELETE /assets/:id/attachments/:fileId
- **验收（CTL-GLOBAL-27）：** 取消无变化；已用于批准的照片历史仍可查看
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-28 · Retry / 重新加载

- **权限：** 页面访问者
- **条件／校验：** 只重试读取或明确未提交的操作
- **动作／目标：** 重新执行失败GET；写入未知结果一律转G01
- **验收（CTL-GLOBAL-28）：** 不产生第二笔交易；保留筛选与非敏感输入
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-29 · Print receipt / Print schedule

- **权限：** 记录访问者
- **条件／校验：** 已记账回执／已批准计划且可访问
- **动作／目标：** 载入专用打印布局，再调用受控打印预览
- **验收（CTL-GLOBAL-29）：** 打印无导航和内部备注，取消打印不改变数据
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-30 · Export CSV / Download export

- **权限：** 授权员工
- **条件／校验：** 当前资源查询权限
- **动作／目标：** GET /transactions/export（其他资源按契约）；服务端导出当前筛选
- **验收（CTL-GLOBAL-30）：** 导出和筛选一致，公式前缀转义、无隐藏敏感字段
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-31 · Return to login / Session expired

- **权限：** 需要登录的访问者
- **条件／校验：** 会话无效
- **动作／目标：** 到C02，returnTo只接受本站允许路由
- **验收（CTL-GLOBAL-31）：** 重新登录不能被returnTo导向恶意外站
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-32 · Return to list / Record not found / Access denied

- **权限：** 当前访问者
- **条件／校验：** 404/403
- **动作／目标：** 清除不可访问详情，回有权限的列表或首页
- **验收（CTL-GLOBAL-32）：** 页面不残留前一客户敏感信息
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### GLOBAL-33 · 确认框 Back to edit

- **权限：** 写操作操作者
- **条件／校验：** 尚未提交
- **动作／目标：** 关闭复核框保留字段和草稿；若已提交进入结果核查
- **验收（CTL-GLOBAL-33）：** 不会误发写请求，不丢表单
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C01 · 客户首页

### C01-01 · How it works

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 滚动至首页流程锚点并聚焦标题
- **验收（CTL-C01-01）：** 顶部与正文两个入口均有效
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C01-02 · My loan / View my loan / Log in

- **权限：** 公开或Customer
- **条件／校验：** 未登录先认证
- **动作／目标：** 未登录到C02；登录客户到C03/C09
- **验收（CTL-C01-02）：** 返回目标安全保存，员工不进入他人客户账户
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C01-03 · Contact / Contact us / Contact our team

- **权限：** 全部
- **条件／校验：** 无
- **动作／目标：** 到C07联系区
- **验收（CTL-C01-03）：** 所有重复CTA指向同一真实帮助页
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C02 · 登录

### C02-01 · Log in

- **权限：** 公开
- **条件／校验：** 邮箱合法、密码非空；服务端限流
- **动作／目标：** POST /auth/login；客户无验证到A04、单笔C03、多笔C09、无关联C10；员工S01
- **验收（CTL-C02-01）：** 错误凭证不泄露账户状态；成功后刷新仍登录
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C02-02 · Forgot password?

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 到A01并可带用户主动输入的邮箱至表单内存
- **验收（CTL-C02-02）：** 不将密码带入URL或日志
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C02-03 · Create an account

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 到A03，注册身份固定Customer
- **验收（CTL-C02-03）：** 客户端传Manager也不能创建员工
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C02-04 · Staff sign-in / Back to home

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 员工入口 /staff/login；返回 /
- **验收（CTL-C02-04）：** 员工登录页不出现员工自助注册
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C03 · 客户个人中心

### C03-01 · View repayment plan

- **权限：** Customer
- **条件／校验：** 当前loanId归本人且已放款
- **动作／目标：** 进入C05 schedule
- **验收（CTL-C03-01）：** 显示当前贷款计划且金额来自API
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C03-02 · Recent repayments View all / 行点击

- **权限：** Customer
- **条件／校验：** 本人贷款
- **动作／目标：** View all到C05 history；行点击打开客户回执
- **验收（CTL-C03-02）：** 回执ID对应点击行；其他用户ID被拒绝
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C03-03 · Your security View details

- **权限：** Customer
- **条件／校验：** 本人资产
- **动作／目标：** 进入C06对应资产／资产列表
- **验收（CTL-C03-03）：** 不显示仓库具体位置和内部备注
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C03-04 · Loan selector / All loans

- **权限：** Customer
- **条件／校验：** 本人多笔贷款
- **动作／目标：** 打开C09或选择本人loanId，清空上一笔详情缓存
- **验收（CTL-C03-04）：** 切换后余额、计划、资产全部一致，不混用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C04 · 申请进度

### C04-01 · Application selector / View application

- **权限：** Customer
- **条件／校验：** 只能本人已提交申请
- **动作／目标：** GET /me/applications并进入选中进度
- **验收（CTL-C04-01）：** 无申请显示空状态，草稿内部备注不可见
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C04-02 · Contact our team

- **权限：** Customer
- **条件／校验：** 无
- **动作／目标：** 到C07
- **验收（CTL-C04-02）：** 已拒绝、待审批、已放款状态均可联系
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C05 · 还款计划与记录

### C05-01 · Upcoming schedule / Payment history

- **权限：** Customer
- **条件／校验：** 当前本人loanId
- **动作／目标：** 切换schedule/history并加载真实接口
- **验收（CTL-C05-01）：** 还款后刷新历史和余额同步
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C05-02 · View receipt / Most recent receipt

- **权限：** Customer
- **条件／校验：** 本人已记账交易
- **动作／目标：** GET /me/receipts/:id打开客户版S13
- **验收（CTL-C05-02）：** 无记录禁用并说明；不显示其他贷款回执
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C05-03 · Contact our team

- **权限：** Customer
- **条件／校验：** 无
- **动作／目标：** 到C07
- **验收（CTL-C05-03）：** 提供真实联系内容，未配置状态明确
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C06 · 客户抵押品详情

### C06-01 · 资产选择 / 图片 / Custody timeline

- **权限：** Customer
- **条件／校验：** 本人关联资产
- **动作／目标：** 资产选择更新详情；图片继承GLOBAL预览；时间线只读
- **验收（CTL-C06-01）：** 已归还与已出售文本区分且无库位泄露
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C06-02 · Contact our team

- **权限：** Customer
- **条件／校验：** 无
- **动作／目标：** 到C07
- **验收（CTL-C06-02）：** 归还等待状态也可进入联系说明
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C07 · 联系与帮助

### C07-01 · FAQ问题 / 展开箭头

- **权限：** 全部
- **条件／校验：** 无
- **动作／目标：** 切换对应答案，aria-expanded及面板关联
- **验收（CTL-C07-01）：** 四个FAQ均能展开收起且键盘可用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C07-02 · View my application

- **权限：** Customer或公开
- **条件／校验：** 公开需先登录；本人申请
- **动作／目标：** 到C04，无申请显示C04空状态
- **验收（CTL-C07-02）：** 不生成新申请、不跳任意示例记录
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C07-03 · 电话 / Email / 地址

- **权限：** 全部
- **条件／校验：** GET /public/contact有已核实配置
- **动作／目标：** tel、mailto、经允许的地图URL；未配置显示说明文本而非假链接
- **验收（CTL-C07-03）：** 不会拨打或邮件至示例联系人
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C07-04 · Back to my overview

- **权限：** Customer或公开
- **条件／校验：** 按会话区分
- **动作／目标：** 客户回C03/C09/C10；公开回C01
- **验收（CTL-C07-04）：** 公开访问不陷入必须登录的返回死循环
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C08 · 客户个人中心手机版

### C08-01 · 手机菜单 / Overview / Progress / Repayments / Help

- **权限：** Customer
- **条件／校验：** 与桌面相同权限
- **动作／目标：** 菜单打开关闭；底部导航到同业务路由；security与account在菜单可达
- **验收（CTL-C08-01）：** 390px下每个入口可点击，无底栏遮挡
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C08-02 · View repayment plan / Latest repayment

- **权限：** Customer
- **条件／校验：** 本人贷款
- **动作／目标：** 复用C03/C05功能而非第二套模拟数据
- **验收（CTL-C08-02）：** 手机与桌面同一ID和金额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C09 · 客户多笔贷款与历史

### C09-01 · Active loans / Loan history

- **权限：** Customer
- **条件／校验：** 本人已关联borrower
- **动作／目标：** GET /me/loans?status=active或history
- **验收（CTL-C09-01）：** 两笔合计来自同一筛选；历史不混入活动总额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C09-02 · View loan

- **权限：** Customer
- **条件／校验：** 行内loanId本人所有
- **动作／目标：** 到C03该贷款详情
- **验收（CTL-C09-02）：** 两行各自打开，不固定第一笔
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C09-03 · View application progress

- **权限：** Customer
- **条件／校验：** 无
- **动作／目标：** 到C04选择申请
- **验收（CTL-C09-03）：** 无申请仍有清楚空状态和联系入口
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## C10 · 新客户与未关联贷款状态

### C10-01 · Contact our team

- **权限：** 已验证Customer
- **条件／校验：** 无关联／无贷款
- **动作／目标：** 到C07说明人工关联和共同申请
- **验收（CTL-C10-01）：** 不把无关联显示成贷款余额0
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### C10-02 · View application progress

- **权限：** Customer
- **条件／校验：** 本人关联范围
- **动作／目标：** 到C04或其空状态
- **验收（CTL-C10-02）：** 没有数据时不展示虚构进度
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S01 · 经理工作台

### S01-01 · Review applications / Awaiting approval / View all applications

- **权限：** Manager；其他员工只读待办
- **条件／校验：** 对应列表权限
- **动作／目标：** 到S03待审核筛选；经理行到S06
- **验收（CTL-S01-01）：** 卡片计数与列表总数同源
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S01-02 · 申请行右箭头

- **权限：** 相关员工
- **条件／校验：** 实际任务分配及角色权限
- **动作／目标：** 待估值到S05，经理待审到S06，草稿贷款专员到S04
- **验收（CTL-S01-02）：** 不会让估值未完成的申请看似可批准
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S01-03 · Recent transactions View all / 行

- **权限：** 财务读取角色
- **条件／校验：** 交易读取范围
- **动作／目标：** 到S13或对应交易回执
- **验收（CTL-S01-03）：** 默认日期范围明确且非固定样例
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S01-04 · Outstanding balance / Active loans

- **权限：** 有贷款读取权限
- **条件／校验：** 如设计为可点击必须显示链接样式
- **动作／目标：** 到S14对应筛选，聚合来自GET /dashboard
- **验收（CTL-S01-04）：** 统计和列表计算口径一致；无数据不显示示例金额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S02 · 客户资料管理

### S02-01 · Add borrower

- **权限：** Loan officer
- **条件／校验：** 无
- **动作／目标：** 进入S20新增表单
- **验收（CTL-S02-01）：** 成功后新客户出现在搜索结果
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S02-02 · Edit details

- **权限：** Loan officer
- **条件／校验：** 借款人存在且version有效
- **动作／目标：** 进入S20编辑，GET /borrowers/:id预填
- **验收（CTL-S02-02）：** 只改选中客户；不会新增重复客户
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S02-03 · Start application

- **权限：** Loan officer
- **条件／校验：** 借款人必需资料完整
- **动作／目标：** POST /applications含borrowerId后到S04
- **验收（CTL-S02-03）：** 重复点击只创建一个草稿
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S02-04 · Details / Loans / View loan

- **权限：** 读取角色
- **条件／校验：** 借款人／贷款范围权限
- **动作／目标：** 详情标签只读资料；Loans查询该borrower贷款；View loan到S08
- **验收（CTL-S02-04）：** 多笔贷款全部可分页查看
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S02-05 · Link account / View linked account

- **权限：** Loan officer
- **条件／校验：** 借款人范围权限
- **动作／目标：** 进入S17并显示当前关联
- **验收（CTL-S02-05）：** 不能仅匹配姓名就开放贷款数据
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S03 · 贷款申请列表

### S03-01 · New application

- **权限：** Loan officer
- **条件／校验：** 无
- **动作／目标：** POST /applications创建草稿，进入borrower步骤
- **验收（CTL-S03-01）：** 无客户时可新增再返回，草稿ID稳定
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S03-02 · 草稿行 / Edit draft

- **权限：** Loan officer
- **条件／校验：** DRAFT，版本未过期
- **动作／目标：** 到S04上次服务端保存步骤
- **验收（CTL-S03-02）：** 重启浏览器仍能恢复已保存内容
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S03-03 · Awaiting valuation / Manager review / Decided

- **权限：** 相应员工
- **条件／校验：** 筛选权限
- **动作／目标：** 查询估值维度／申请状态；Disbursed由关联贷款状态衍生
- **验收（CTL-S03-03）：** 草稿显示修改时间而非假提交日期
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S04 · 贷款申请填写

### S04-01 · Borrower搜索 / Select borrower

- **权限：** Loan officer
- **条件／校验：** 已保存的有效borrower；同一草稿未提交
- **动作／目标：** GET /borrowers?q=，PATCH /applications/:id保存borrowerId
- **验收（CTL-S04-01）：** 切换borrower提示关联内容影响，刷新仍选中正确客户
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S04-02 · Add new borrower

- **权限：** Loan officer
- **条件／校验：** 无
- **动作／目标：** 打开S20，保存后返回当前草稿并选中
- **验收（CTL-S04-02）：** 不会丢当前草稿
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S04-03 · Save draft

- **权限：** Loan officer
- **条件／校验：** DRAFT；允许缺必填但已填数据合法
- **动作／目标：** PATCH /applications/:id含expectedVersion
- **验收（CTL-S04-03）：** 刷新恢复；409保留输入并说明有新版本
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S04-04 · Continue

- **权限：** Loan officer
- **条件／校验：** 当前步骤必填与格式正确
- **动作／目标：** 保存当前步骤后到下一步；字段错误聚焦首项
- **验收（CTL-S04-04）：** 保存失败不跳步，已填信息不丢
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S04-05 · Back / 五步Step导航

- **权限：** Loan officer
- **条件／校验：** 离开脏表单先保存或确认；未提交
- **动作／目标：** 回前步／已访问步骤；跨步不绕过提交校验
- **验收（CTL-S04-05）：** 最终review仍检查全部步骤
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S04-06 · Manage security assets

- **权限：** Loan officer
- **条件／校验：** DRAFT
- **动作／目标：** 到S21 security步骤
- **验收（CTL-S04-06）：** 当前申请ID保持不变
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S05 · 抵押品估值

### S05-01 · Asset selector / Valuation photos

- **权限：** Valuation officer；相关员工只读
- **条件／校验：** 分配的估值任务
- **动作／目标：** 选择当前任务资产；上传继承GLOBAL且绑定估值版本
- **验收（CTL-S05-01）：** 不把照片写到另一申请
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S05-02 · Save draft

- **权限：** Valuation officer
- **条件／校验：** 任务REQUESTED或IN_PROGRESS；已填合法
- **动作／目标：** POST /valuations或PATCH /valuations/:id草稿
- **验收（CTL-S05-02）：** 不显示Valuation completed，不开启审批资格
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S05-03 · Save valuation / Complete valuation

- **权限：** Valuation officer
- **条件／校验：** 金额、依据、日期、三方参与记录齐全
- **动作／目标：** POST /valuations/:id/complete保存完整快照
- **验收（CTL-S05-03）：** 缺任一参与者422；完成后申请readiness更新
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S05-04 · 参与人选择 / Present记录

- **权限：** Valuation officer
- **条件／校验：** 人员属于正确角色；借款人为当前申请对象
- **动作／目标：** 表单记录实际参与人员、方式、时间，随估值保存
- **验收（CTL-S05-04）：** 不是固定姓名或默认全部Present
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S06 · 经理审批详情

### S06-01 · Application / Valuation / Repayment terms / History

- **权限：** Manager及允许只读员工
- **条件／校验：** 申请读取权限
- **动作／目标：** 加载提交快照与历史，标签不改状态
- **验收（CTL-S06-01）：** 经理看到的版本与提交一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S06-02 · View details / Open repayment terms

- **权限：** Manager及允许只读员工
- **条件／校验：** 对应记录权限
- **动作／目标：** 打开S05或S15只读模式
- **验收（CTL-S06-02）：** 审批中不能偷偷修改原条款
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S06-03 · Review checklist复选框

- **权限：** Manager
- **条件／校验：** SUBMITTED且ready
- **动作／目标：** 本次复核确认；不代替服务器前置校验
- **验收（CTL-S06-03）：** 未勾全禁用批准；API仍复核readiness
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S06-04 · Approve application

- **权限：** Manager
- **条件／校验：** SUBMITTED、ready、已复核、expectedVersion
- **动作／目标：** 打开批准确认，展示金额、资产和计划摘要
- **验收（CTL-S06-04）：** 尚未最终确认时没有审批写入
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S06-05 · Confirm approval

- **权限：** Manager
- **条件／校验：** 同上且快照未变
- **动作／目标：** POST /applications/:id/decision decision=approve
- **验收（CTL-S06-05）：** 一次决定、生成未放款贷款，不产生放款交易
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S06-06 · Decline / Confirm decline

- **权限：** Manager
- **条件／校验：** SUBMITTED；拒绝原因必填1–1000；客户公开说明独立
- **动作／目标：** 打开拒绝理由表单，确认POST decision=decline
- **验收（CTL-S06-06）：** 客户只看到公开说明；经理内部备注不外泄
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S07 · 抵押品仓库与入库

### S07-01 · Receive security / 行进入入库

- **权限：** Valuation officer
- **条件／校验：** 批准关联贷款；资产未STORED/RETURNED/SOLD
- **动作／目标：** 打开入库抽屉，GET /assets/:id
- **验收（CTL-S07-01）：** 列出收到、检查、位置三个条件
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S07-02 · Confirm intake

- **权限：** Valuation officer
- **条件／校验：** 收到日期、经办人、检查合格、符合估值记录、库位齐全
- **动作／目标：** POST /assets/:id/intake带version
- **验收（CTL-S07-02）：** 原子写入交接事件；放款条件即时反映
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S07-03 · 检查不合格 / Report mismatch

- **权限：** Valuation officer
- **条件／校验：** 实际不符合；原因必填
- **动作／目标：** 保存inspection异常，资产保持待处理并通知贷款专员／经理
- **验收（CTL-S07-03）：** 不能勾选不匹配仍完成合格入库
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S07-04 · Update storage / Update condition

- **权限：** Valuation officer
- **条件／校验：** 资产STORED；理由必填
- **动作／目标：** PATCH /assets/:id/custody追加位置／检查事件
- **验收（CTL-S07-04）：** 旧库位历史保留，客户界面不见库位
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S07-05 · Return security / Record sale

- **权限：** Valuation officer
- **条件／校验：** 服务器allowedActions允许
- **动作／目标：** 分别到S11/S12；不满足给禁用原因
- **验收（CTL-S07-05）：** 非结清不可归还，非违约不可出售
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S08 · 贷款账户详情

### S08-01 · View repayment schedule / Account tabs

- **权限：** 贷款读取角色
- **条件／校验：** 对应贷款权限
- **动作／目标：** 加载schedule、transactions、security、history
- **验收（CTL-S08-01）：** 各标签同loanId，历史显示实际作者时间
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-02 · View application

- **权限：** 贷款读取角色
- **条件／校验：** 关联申请可读
- **动作／目标：** 到S06只读或有权决策模式
- **验收（CTL-S08-02）：** 资金已放出不能再显示批准按钮
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-03 · View asset / View all transactions

- **权限：** 贷款读取角色
- **条件／校验：** 相应读取范围
- **动作／目标：** 到S07资产详情／S13限定loanId
- **验收（CTL-S08-03）：** 返回贷款时保留标签
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-04 · Disburse loan

- **权限：** Cashier
- **条件／校验：** APPROVED_UNFUNDED及所有资产条件满足
- **动作／目标：** 到S09；不满足显示原因及可读资产入口
- **验收（CTL-S08-04）：** 经理不能调用此POST
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-05 · Record repayment

- **权限：** Cashier
- **条件／校验：** 已放款、无相关未决attempt、规则允许收款
- **动作／目标：** 到S10
- **验收（CTL-S08-05）：** 已结清按钮不可用，原因明确
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-06 · Declare default / Confirm default

- **权限：** Manager
- **条件／校验：** 已放款；正式违约政策有效；原因和依据必填
- **动作／目标：** 复核后POST /loans/:id/default
- **验收（CTL-S08-06）：** 逾期不自动违约；审计记录决定者和依据
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S08-07 · Settlement / Resolve sale balance

- **权限：** Manager审核、Cashier执行
- **条件／校验：** 已出售、收款已记账、差额政策有效
- **动作／目标：** GET /loans/:id/quotes?type=settlement；显示差额及允许动作；复核POST settlement-actions
- **验收（CTL-S08-07）：** 规则缺失显示待结算；不偷偷把欠款归零
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S09 · 出纳放款

### S09-01 · Review disbursement

- **权限：** Cashier
- **条件／校验：** 批准、收到、检查、存储、未放款；方式及日期合法
- **动作／目标：** 服务端预检与quote后打开复核面板
- **验收（CTL-S09-01）：** 审批撤销／并发放款会被服务器拒绝
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S09-02 · Confirm disbursement

- **权限：** Cashier
- **条件／校验：** quote及version有效；创建attemptId
- **动作／目标：** POST /loans/:id/disbursements含Idempotency-Key
- **验收（CTL-S09-02）：** 重复请求返回同交易与回执；贷款变ACTIVE一次
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S09-03 · Cancel / Back to edit

- **权限：** Cashier
- **条件／校验：** 尚未提交
- **动作／目标：** 取消回S08；编辑回录入表单
- **验收（CTL-S09-03）：** 不记账；已提交超时必须走G01
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S10 · 出纳收款与确认

### S10-01 · Loan选择 / Payment method / Received on

- **权限：** Cashier
- **条件／校验：** 选中可收款贷款；日期方式在政策范围
- **动作／目标：** 选贷款GET余额，改变金额或日期使旧quote失效
- **验收（CTL-S10-01）：** 不能用旧贷款余额核算新贷款还款
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S10-02 · Review repayment

- **权限：** Cashier
- **条件／校验：** 金额>0、小数≤2位；规则支持部分/超额/提前与业务日期
- **动作／目标：** GET /loans/:id/quotes?type=repayment并打开复核
- **验收（CTL-S10-02）：** 展示服务端计算的分配与结余；不前端减法冒充
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S10-03 · Confirm repayment

- **权限：** Cashier
- **条件／校验：** quote和version有效，无未决意图
- **动作／目标：** POST /loans/:id/repayments含幂等key
- **验收（CTL-S10-03）：** 余额、计划、账本、回执一次提交；超时转G01
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S10-04 · Back to edit / Cancel

- **权限：** Cashier
- **条件／校验：** 未提交
- **动作／目标：** 返回编辑／S08；更改后必须重新quote
- **验收（CTL-S10-04）：** 不重用失效确认数据
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S10-05 · View receipt / Record another payment

- **权限：** Cashier
- **条件／校验：** 前笔已确认committed或明确rejected
- **动作／目标：** 回执到S13；新收款重新载入余额并生成新意图
- **验收（CTL-S10-05）：** pending时Record another payment禁用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S11 · 结清与抵押品归还

### S11-01 · Borrower and asset checked复选框

- **权限：** Valuation officer
- **条件／校验：** 完成实际借款人、资产身份核对
- **动作／目标：** 记录核对方法及对象，影响review资格
- **验收（CTL-S11-01）：** 未勾选不得最终归还
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S11-02 · Review return

- **权限：** Valuation officer
- **条件／校验：** 余额0、SETTLED、资产STORED、无更正/处置锁；交接信息齐全
- **动作／目标：** 服务端预检后显示资产、接收人和交接摘要
- **验收（CTL-S11-02）：** 余额变化立即使确认资格失效
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S11-03 · Confirm return

- **权限：** Valuation officer
- **条件／校验：** 最新资格、version有效
- **动作／目标：** POST /assets/:id/return
- **验收（CTL-S11-03）：** 同资产仅一次归还；库存与客户时间线一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S11-04 · Back to asset

- **权限：** Valuation officer
- **条件／校验：** 脏表单保护
- **动作／目标：** 回S07详情
- **验收（CTL-S11-04）：** 没有静默提交或丢失已保存交接记录
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S12 · 违约与抵押品出售

### S12-01 · Save draft

- **权限：** Valuation officer
- **条件／校验：** DEFAULTED且资产仍可出售；已填字段合法
- **动作／目标：** 保存sale草稿 POST/PATCH /assets/:id/sale-draft
- **验收（CTL-S12-01）：** 不标记SOLD，不产生资金流水
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S12-02 · Linked cashier receipt下拉

- **权限：** Valuation officer
- **条件／校验：** 仅同贷款、同处置、未被其他处置使用的sale receipt
- **动作／目标：** GET /transactions?type=sale_proceeds&loanId=并选择或留待收款
- **验收（CTL-S12-02）：** 金额/贷款不匹配禁止关联，不能用还款回执充当出售收入
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S12-03 · Review sale

- **权限：** Valuation officer
- **条件／校验：** 已违约、资产在库、买方/金额/日期资料齐全
- **动作／目标：** 预检后展示出售复核；无回执明确等待出纳收款
- **验收（CTL-S12-03）：** 不把未收款销售显示为已结算
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S12-04 · Confirm sale

- **权限：** Valuation officer
- **条件／校验：** version有效，未归还/出售
- **动作／目标：** POST /assets/:id/sale；附可选有效receiptId
- **验收（CTL-S12-04）：** 实物SOLD并保留审计；不自动冲减余额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S12-05 · Record sale proceeds / Confirm sale receipt

- **权限：** Cashier
- **条件／校验：** 已有sale记录，金额匹配或政策允许的明确差异
- **动作／目标：** 复用S10录入/复核；POST /loans/:id/sale-receipts幂等记账
- **验收（CTL-S12-05）：** 估值专员不能收款；超时走G01；余款/不足保持待处理
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S13 · 交易流水与回执

### S13-01 · 交易行 / View receipt

- **权限：** 交易读取角色
- **条件／校验：** 读取范围
- **动作／目标：** GET /transactions/:id和/receipt打开抽屉
- **验收（CTL-S13-01）：** 原交易与更正关联都可查，不能编辑原金额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S13-02 · Print receipt / Close

- **权限：** 交易读取角色
- **条件／校验：** 已记账回执
- **动作／目标：** 继承GLOBAL打印及关闭，客户使用客户版布局
- **验收（CTL-S13-02）：** 回执信息与数据库一致，无内部备注泄露
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S13-03 · Request correction

- **权限：** Cashier
- **条件／校验：** 支持更正的已记账交易，无同笔待处理更正
- **动作／目标：** 打开S18申请表，显示原记录，只能填写替代值与理由
- **验收（CTL-S13-03）：** 不出现直接Edit/Delete已记账操作
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S13-04 · Corrections / 更正状态链接

- **权限：** 相关员工
- **条件／校验：** 更正读取权限
- **动作／目标：** 到S18列表或指定更正详情
- **验收（CTL-S13-04）：** 原回执上可追溯更正编号
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S13-05 · Export transactions

- **权限：** 财务读取角色
- **条件／校验：** 当前筛选及导出权限
- **动作／目标：** 继承GLOBAL导出GET /transactions/export
- **验收（CTL-S13-05）：** 空结果导出带表头，范围与界面一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S14 · 贷款总列表

### S14-01 · 贷款行 / 状态标签 / 筛选

- **权限：** 贷款读取角色
- **条件／校验：** 读取范围
- **动作／目标：** 到S08；筛选按active/overdue/default/settled分别查询
- **验收（CTL-S14-01）：** overdue不被当成default；0余额不自动等于资产已归还
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S15 · 还款条款与计划预览

### S15-01 · 频率 / 首期日期 / 期数 / 利息方法 / 费率周期

- **权限：** Loan officer
- **条件／校验：** DRAFT；只能选已批准政策允许值
- **动作／目标：** 编辑terms草稿；变更任一项使旧preview失效
- **验收（CTL-S15-01）：** 没有政策显示未配置，不能默认图片利率
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S15-02 · Add fee

- **权限：** Loan officer
- **条件／校验：** DRAFT；正式政策允许该费用
- **动作／目标：** 增加费用行，名称、金额、时点等必填
- **验收（CTL-S15-02）：** 空行不能preview；禁止负数或未经政策允许的费用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S15-03 · Edit fee / Remove fee

- **权限：** Loan officer
- **条件／校验：** DRAFT；不是已批准快照
- **动作／目标：** 编辑费用／确认移除并失效旧preview
- **验收（CTL-S15-03）：** 预览重新计算；历史批准费用不改变
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S15-04 · Preview schedule

- **权限：** Loan officer
- **条件／校验：** 政策、金额、周期、日期、期数和费用齐全
- **动作／目标：** POST /applications/:id/schedule-preview
- **验收（CTL-S15-04）：** 返回每期明细及总额，版本绑定输入，精度与日期边界可测试
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S15-05 · Save draft

- **权限：** Loan officer
- **条件／校验：** DRAFT；已填合法
- **动作／目标：** PUT /applications/:id/terms含version
- **验收（CTL-S15-05）：** 刷新保存参数；未生成计划仍不能提交
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S15-06 · Continue

- **权限：** Loan officer
- **条件／校验：** 最新preview有效且已保存terms
- **动作／目标：** 保存并进入S22
- **验收（CTL-S15-06）：** 修改费率后不能沿用旧preview继续
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S16 · 员工账户与邀请

### S16-01 · Invite staff

- **权限：** manage_staff
- **条件／校验：** 有效管理权限
- **动作／目标：** 打开邀请抽屉
- **验收（CTL-S16-01）：** 无临时密码字段，不创建公开管理员注册入口
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-02 · Send invitation

- **权限：** manage_staff
- **条件／校验：** 姓名、邮箱、角色有效；不能重复活跃邮箱
- **动作／目标：** POST /staff含业务role；后端发激活邀请
- **验收（CTL-S16-02）：** 创建invited而非active；邮件失败可重发且无假成功
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-03 · 行菜单 / View details

- **权限：** manage_staff
- **条件／校验：** 对象存在
- **动作／目标：** 查看角色、状态、邀请时间及权限，不显示token
- **验收（CTL-S16-03）：** 活动用户Resend invitation禁用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-04 · Change role / Confirm role change

- **权限：** manage_staff
- **条件／校验：** 非自己提升权限；保留至少一名管理员；理由必填
- **动作／目标：** PATCH /staff/:id/role并审计、撤销旧会话
- **验收（CTL-S16-04）：** 旧token即刻不能执行原角色权限
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-05 · Manage permissions / Save permissions

- **权限：** manage_staff
- **条件／校验：** 仅manage_staff/view_audit；不能自我授权或移除最后管理员
- **动作／目标：** PATCH /staff/:id/permissions；复核名单和理由
- **验收（CTL-S16-05）：** 不会隐式赋予Manager或Cashier业务角色
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-06 · Deactivate / Confirm deactivation

- **权限：** manage_staff
- **条件／校验：** 非最后有效管理员；理由必填
- **动作／目标：** PATCH /staff/:id/status inactive
- **验收（CTL-S16-06）：** 即刻撤销会话；历史交易作者仍存在
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-07 · Reactivate / Confirm reactivation

- **权限：** manage_staff
- **条件／校验：** inactive且角色权限合法；理由必填
- **动作／目标：** PATCH /staff/:id/status active
- **验收（CTL-S16-07）：** 必须重新登录；不恢复旧会话
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S16-08 · Resend invitation / Revoke invitation

- **权限：** manage_staff
- **条件／校验：** invited且限流允许
- **动作／目标：** POST /staff/:id/resend-invitation；撤销PATCH status=invitation_revoked
- **验收（CTL-S16-08）：** 旧token失效；撤销后受邀人不能激活
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S17 · 客户账户与借款人关联

### S17-01 · Search verified account / Search borrower

- **权限：** Loan officer
- **条件／校验：** 仅已验证且无冲突关联对象
- **动作／目标：** GET /verified-accounts?q=或borrowers?q=，加载比对信息
- **验收（CTL-S17-01）：** 不能列出密码和token；不自动根据同名关联
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S17-02 · Verification method / Notes / Confirm identity复选框

- **权限：** Loan officer
- **条件／校验：** 实际人工核对、notes必填
- **动作／目标：** 收集核对证据摘要，不上传额外证件号码
- **验收（CTL-S17-02）：** 无核对方法或未勾选时Review link不可用
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S17-03 · Review link

- **权限：** Loan officer
- **条件／校验：** 账户已验证、borrower存在、双方无有效冲突关联
- **动作／目标：** 展示双边ID和姓名/邮箱确认对话框
- **验收（CTL-S17-03）：** 复核期间后台关联变化会409
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S17-04 · Confirm link

- **权限：** Loan officer
- **条件／校验：** 同上，最新version
- **动作／目标：** POST /borrowers/:id/account-link
- **验收（CTL-S17-04）：** 刷新客户可看该borrower贷款；不看到其他borrower
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S17-05 · Revoke incorrect link / Confirm revoke

- **权限：** Manager
- **条件／校验：** 已有关系；理由必填；二次确认
- **动作／目标：** POST /borrowers/:id/account-link/revoke
- **验收（CTL-S17-05）：** 旧客户立即失去数据访问，关联历史保留
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S18 · 交易更正申请与审核

### S18-01 · New correction / Submit request

- **权限：** Cashier
- **条件／校验：** 原交易可更正；替代金额>0、理由必填；无重复pending
- **动作／目标：** POST /corrections含originalTxnId/proposedValues
- **验收（CTL-S18-01）：** 请求pending，余额尚未变化
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S18-02 · View original receipt

- **权限：** 相关员工
- **条件／校验：** 原交易可读
- **动作／目标：** 打开S13只读原回执
- **验收（CTL-S18-02）：** 任何审批状态都能追溯原交易
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S18-03 · Approve correction / Confirm approval

- **权限：** Manager
- **条件／校验：** REQUESTED；不可自审；重算政策支持；版本有效
- **动作／目标：** 复核前后差异，POST /corrections/:id/decision approve
- **验收（CTL-S18-03）：** 只变approved，不立刻记账
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S18-04 · Reject / Confirm reject

- **权限：** Manager
- **条件／校验：** REQUESTED，拒绝理由必填
- **动作／目标：** POST /corrections/:id/decision reject
- **验收（CTL-S18-04）：** 余额不变，出纳看到拒绝原因
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S18-05 · Post correction / Confirm posting

- **权限：** Cashier
- **条件／校验：** APPROVED，原交易仍可更正，相关资产未受不可逆交接影响
- **动作／目标：** POST /corrections/:id/post含幂等key
- **验收（CTL-S18-05）：** 冲正与替代原子提交，重复点击只产生一组更正
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S18-06 · 更正列表状态 / 行入口

- **权限：** Cashier/Manager/Accountant有范围权限
- **条件／校验：** 有更正读权限
- **动作／目标：** GET /corrections，选择进入详情
- **验收（CTL-S18-06）：** 权限决定申请/审核/执行按钮而非图片固定按钮
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S19 · 操作日志与变更详情

### S19-01 · Action / Date / User或Record搜索

- **权限：** view_audit
- **条件／校验：** 权限和参数合法
- **动作／目标：** GET /audit-events带筛选
- **验收（CTL-S19-01）：** 审计总数分页正确，未授权用户403
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S19-02 · 审计行 / Details

- **权限：** view_audit
- **条件／校验：** 事件可读
- **动作／目标：** GET /audit-events/:id显示actor/time/before/after/reason
- **验收（CTL-S19-02）：** 只读无删除编辑，不返回token密码
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S19-03 · View borrower / View related record

- **权限：** view_audit加目标读取权限
- **条件／校验：** 目标记录仍可读
- **动作／目标：** 跳目标详情；否则禁用并解释
- **验收（CTL-S19-03）：** 审计权限不绕过业务记录权限
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S20 · 新增与编辑借款人

### S20-01 · Save borrower

- **权限：** Loan officer
- **条件／校验：** 姓名、电话、地址必填及长度规则；邮箱可选合法
- **动作／目标：** POST /borrowers
- **验收（CTL-S20-01）：** 生成ID并返回S02新记录或原申请，刷新可查
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S20-02 · Save changes

- **权限：** Loan officer
- **条件／校验：** 同上及expectedVersion
- **动作／目标：** PATCH /borrowers/:id
- **验收（CTL-S20-02）：** 旧值进审计；客户账户邮箱不被借款人邮箱编辑覆盖
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S20-03 · Cancel

- **权限：** Loan officer
- **条件／校验：** 脏表单保护
- **动作／目标：** 回来源详情／申请
- **验收（CTL-S20-03）：** 取消不创建半成品借款人
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S21 · 申请抵押品资料与照片

### S21-01 · Add asset

- **权限：** Loan officer
- **条件／校验：** DRAFT
- **动作／目标：** 打开空资产抽屉
- **验收（CTL-S21-01）：** 名称描述状况字段完整、无自行输入估值金额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-02 · Save asset

- **权限：** Loan officer
- **条件／校验：** 字段合法；当前申请DRAFT
- **动作／目标：** POST /assets含applicationId
- **验收（CTL-S21-02）：** 保存到该草稿，刷新存在，尚未估值明确提示
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-03 · Edit / Save changes

- **权限：** Loan officer
- **条件／校验：** DRAFT；asset版本有效
- **动作／目标：** PATCH /assets/:id；实质变更使估值失效
- **验收（CTL-S21-03）：** 不能保留与新资产资料不一致的已完成估值
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-04 · Remove / Confirm remove

- **权限：** Loan officer
- **条件／校验：** 未提交、未入库、未绑定有效贷款
- **动作／目标：** DELETE /applications/:id/assets/:assetId草稿关系
- **验收（CTL-S21-04）：** 仅移除草稿关系，留历史审计，不删除已抵押实物
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-05 · View valuation

- **权限：** Loan officer只读/Valuation officer可编辑
- **条件／校验：** 当前资产估值范围
- **动作／目标：** 有估值打开S05；无估值显示未开始及Request valuation入口
- **验收（CTL-S21-05）：** 不能显示假完成估值
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-06 · Request valuation

- **权限：** Loan officer
- **条件／校验：** 资产资料完整、borrower及拟定金额已保存
- **动作／目标：** POST /applications/:id/valuation-request分配估值人员
- **验收（CTL-S21-06）：** 估值人员收到任务；重复请求不重复创建
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S21-07 · Back / Save draft / Continue

- **权限：** Loan officer
- **条件／校验：** 继承S04；至少一件合法资产才能继续
- **动作／目标：** 保存后前后步骤跳转
- **验收（CTL-S21-07）：** 资产抽屉未保存时先提示，申请ID保持一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## S22 · 申请复核与提交条件

### S22-01 · Edit borrower / Edit loan / Edit security / Edit terms

- **权限：** Loan officer
- **条件／校验：** DRAFT
- **动作／目标：** 返回对应step并定位字段
- **验收（CTL-S22-01）：** 编辑后返回review重新加载readiness
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S22-02 · View valuation / View terms

- **权限：** Loan officer
- **条件／校验：** 相关数据可读
- **动作／目标：** 打开S05只读／S15预览
- **验收（CTL-S22-02）：** 不让贷款专员完成估值或经理决定
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S22-03 · Readiness缺失项目链接

- **权限：** Loan officer
- **条件／校验：** 后端GET /applications/:id/readiness
- **动作／目标：** 跳至缺失字段或任务；政策未配置解释依赖
- **验收（CTL-S22-03）：** 所有缺项可定位，不只有灰按钮
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S22-04 · Submit for review / Confirm submission

- **权限：** Loan officer
- **条件／校验：** readiness全部满足、三方估值完整、terms及schedule版本一致
- **动作／目标：** POST /applications/:id/submit；冻结快照
- **验收（CTL-S22-04）：** 变SUBMITTED，重复提交不新建申请；后台再校验全部条件
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### S22-05 · Back / Save draft

- **权限：** Loan officer
- **条件／校验：** DRAFT
- **动作／目标：** 复用S04存草稿及返回terms
- **验收（CTL-S22-05）：** 状态不会提前变SUBMITTED
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A01 · 密码恢复

### A01-01 · Send reset link

- **权限：** 公开
- **条件／校验：** 邮箱合法；服务端限流
- **动作／目标：** POST /auth/password/forgot，显示中性结果
- **验收（CTL-A01-01）：** 存在/不存在邮箱提示一致；不能暴露用户资料
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A01-02 · Back to login / Contact our team

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 分别到C02/C07
- **验收（CTL-A01-02）：** 过期或发送失败后有可操作返回入口
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A02 · 账户资料与安全

### A02-01 · Update password

- **权限：** 已登录
- **条件／校验：** 当前密码正确，新密码12–128字符且确认一致
- **动作／目标：** POST /auth/password/change；撤销旧会话并回C02
- **验收（CTL-A02-01）：** 错误密码失败不更改；成功后旧会话失效
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A02-02 · Contact our team / Back to overview / Sign out

- **权限：** 已登录
- **条件／校验：** 对应共用权限
- **动作／目标：** 到C07／所属首页／POST logout
- **验收（CTL-A02-02）：** 角色及账户邮箱在此只读，不混同借款人资料编辑
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A03 · 客户邮箱注册

### A03-01 · Create account

- **权限：** 公开
- **条件／校验：** 姓名、邮箱、密码、确认密码合法
- **动作／目标：** POST /auth/register；身份固定Customer，建立受限待验证会话到A04
- **验收（CTL-A03-01）：** 双击同邮箱不重复账户；不生成loan/application
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A03-02 · Log in / Back to home

- **权限：** 公开
- **条件／校验：** 无
- **动作／目标：** 分别C02/C01
- **验收（CTL-A03-02）：** 保留页面路由正确，不携带密码
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A04 · 等待邮箱验证

### A04-01 · Resend verification email

- **权限：** 待验证Customer
- **条件／校验：** 受限待验证会话；服务端retryAfter到期
- **动作／目标：** POST /auth/email/resend；旧链接失效
- **验收（CTL-A04-01）：** 按钮真实倒计时；发送失败不显示已发送
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A04-02 · Use a different email / Save email

- **权限：** 待验证Customer
- **条件／校验：** 仅当前注册会话，邮箱合法且无冲突
- **动作／目标：** 打开邮箱编辑；POST /auth/email/change-pending；重发新链接
- **验收（CTL-A04-02）：** 旧邮箱链接失效，新地址掩码正确
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A04-03 · Back to login

- **权限：** 待验证或公开
- **条件／校验：** 无
- **动作／目标：** 到C02，可清除受限会话
- **验收（CTL-A04-03）：** 未验证账号不能靠URL进客户数据页
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A05 · 邮箱验证结果

### A05-01 · 验证链接加载 / Retry verification

- **权限：** 链接持有者
- **条件／校验：** token有效、未过期；单次消费
- **动作／目标：** 显式POST /auth/email/verify token；呈现成功/已验证/过期/无效
- **验收（CTL-A05-01）：** GET页面本身不直接变更；重放不创建完整登录会话
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A05-02 · Continue to login / Back to login

- **权限：** 全部
- **条件／校验：** 服务端验证结果已返回
- **动作／目标：** 到C02
- **验收（CTL-A05-02）：** 成功不自动提升为登录会话
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A05-03 · Request a new link

- **权限：** 过期或无效链接持有者
- **条件／校验：** 有待验证会话可直接重发，否则回登录取得受限会话
- **动作／目标：** 到A04或C02，继承发送限流
- **验收（CTL-A05-03）：** 不凭不可信token开放任意邮箱修改
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A05-04 · Contact our team

- **权限：** 全部
- **条件／校验：** 无
- **动作／目标：** 到C07
- **验收（CTL-A05-04）：** 验证失败也能获得帮助
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A06 · 员工接受邀请与激活

### A06-01 · Activate account

- **权限：** 受邀员工
- **条件／校验：** token有效，密码合法一致，invitation仍invited
- **动作／目标：** POST /auth/invitations/accept；角色从服务端邀请读取
- **验收（CTL-A06-01）：** 单次激活；客户端修改role无效；成功回staff/login
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A06-02 · Back to staff login / Request a new invitation

- **权限：** 受邀人
- **条件／校验：** 邀请过期或撤销时不可激活
- **动作／目标：** 到staff/login；过期页说明联系管理员，到C07
- **验收（CTL-A06-02）：** 不能自行续期或重新选择角色
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## A07 · 设置新密码

### A07-01 · Reset password

- **权限：** 重置链接持有者
- **条件／校验：** token有效，新密码及确认合法
- **动作／目标：** POST /auth/password/reset；失效token和所有旧会话
- **验收（CTL-A07-01）：** 一次生效；旧密码失败、新密码成功
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### A07-02 · Request a new link / Back to login

- **权限：** 公开
- **条件／校验：** 重置成功／链接无效或过期
- **动作／目标：** 到A01／C02
- **验收（CTL-A07-02）：** 无效链接不会泄露邮箱和用户资料
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## D01 · 桌面端连接中断

### D01-01 · Retry connection

- **权限：** 员工桌面
- **条件／校验：** 断线或health失败；不写资金
- **动作／目标：** GET /health并验证会话，加载当前服务端状态和unresolved attempts
- **验收（CTL-D01-01）：** 断线恢复不自动新建资金交易
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### D01-02 · Return to overview

- **权限：** 员工桌面
- **条件／校验：** 可离开当前页；未决资金保留服务端记录
- **动作／目标：** 显示工作台壳及离线状态；恢复后查询真实数据
- **验收（CTL-D01-02）：** 不显示过时缓存为当前余额
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### D01-03 · 原生最小化 / 最大化或还原 / 关闭

- **权限：** 桌面用户
- **条件／校验：** Electron窗口存在；脏表单提示
- **动作／目标：** 使用系统窗口行为；关闭不撤销已发送请求
- **验收（CTL-D01-03）：** 重开后可查未决attempt，没有离线补发队列
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## G01 · 交易结果未知与恢复

### G01-01 · Check payment status

- **权限：** 该意图操作者或授权出纳
- **条件／校验：** attempt属于可访问范围
- **动作／目标：** GET /payment-attempts/:id；pending继续、committed回原回执、rejected提示修复
- **验收（CTL-G01-01）：** 未知/404绝不直接当成未记账
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G01-02 · View transaction history / Back to transactions

- **权限：** Cashier
- **条件／校验：** 对应贷款交易范围
- **动作／目标：** 到S13限定loanId并保留未决提醒
- **验收（CTL-G01-02）：** 返回后仍可核查同attempt，不产生新key
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G01-03 · Record another payment

- **权限：** Cashier
- **条件／校验：** 前笔明确committed或rejected且业务允许
- **动作／目标：** 重新加载余额和新表单到S10
- **验收（CTL-G01-03）：** pending一直禁用且有原因
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G01-04 · Retry original attempt / 恢复核查

- **权限：** Cashier
- **条件／校验：** 服务器暂查不到结果；有原请求摘要
- **动作／目标：** 同key同body幂等恢复，不创建新意图
- **验收（CTL-G01-04）：** 请求曾成功也只返回原交易；body变更409
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

## G02 · 通知与待办入口

### G02-01 · All / Unread

- **权限：** 已登录
- **条件／校验：** 自己的通知
- **动作／目标：** GET /notifications?read=all或false
- **验收（CTL-G02-01）：** 切换后数量与列表一致
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G02-02 · View application / View correction / View asset

- **权限：** 相应授权用户
- **条件／校验：** 目标当前仍可见
- **动作／目标：** 通知关联真实recordId路由，进入后按设计标已读
- **验收（CTL-G02-02）：** 不能用通知链接绕过权限；目标不可用有返回
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G02-03 · Mark as read

- **权限：** 通知接收者
- **条件／校验：** 本人通知
- **动作／目标：** POST /notifications/:id/read
- **验收（CTL-G02-03）：** 跨设备刷新保持已读，未读数量减少一次
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

### G02-04 · Mark all as read

- **权限：** 通知接收者
- **条件／校验：** 本人当前可见通知范围
- **动作／目标：** POST /notifications/read-all带截止时间
- **验收（CTL-G02-04）：** 请求期间新增通知不被误标已读
- **异常：** 遵循 IMPLEMENTATION_SPEC §4/§6：字段422就地提示、409保留输入并刷新比对、401重新登录、403/404不泄露记录、429按服务器等待、读取失败可重试；资金写入结果不明进入G01。

